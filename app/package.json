{
  "name": "apostasynft",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "copyfiles": "^2.4.1",
    "env-cmd": "^10.1.0",
    "ethers": "^6.3.0",
    "firebase-admin": "^11.7.0",
    "jquery": "^3.6.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.0",
    "react-scripts": "5.0.1",
    "rimraf": "^5.0.0",
    "use-sound": "^4.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "node app/server.js",
    "build": "react-scripts build",
    "release": "npm run build && copyfiles server.js app && copyfiles package.json app && copyfiles fbase-key.json app && copyfiles build/**/* app", 
    "test": "npm run build && npm run server",
    "oldtest": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
